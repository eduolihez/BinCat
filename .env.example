# Copia este archivo como .env y genera tu propia clave.
# BinCat genera una automáticamente si no existe (ver tools/encryption_key_gen.py),
# pero nunca commitees el .env real: contiene la clave que cifra tus tokens.
ENCRYPTION_KEY=
